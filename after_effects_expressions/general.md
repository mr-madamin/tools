# General

## Value

### Control keyframes when expression is set

You can control the value even with expression set. Just add `+ value`.

```js
time + value
```

### Switch from positive to negative value in a quick way

```js
value * -1
```
